System containing the tets for the SIFT image matching algorithim
clone the repo, open in vs code, execute it using by changing the local variables
the system uses cv2 v.4.5.1, so make sure yout python libs are updated 
